apiVersion: v1
kind: Service
metadata:
  name: {{ include "fenghuo.fullname" . }}
  labels:
    {{- include "fenghuo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
  selector:
    {{- include "fenghuo.selectorLabels" . | nindent 4 }}
